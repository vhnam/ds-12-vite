import { Meta } from "@storybook/addon-docs/blocks";
import { FoundationRadiusDoc } from "./foundation-radius-doc.tsx";

<Meta title="Foundations/Radius/Foundation Radius" />

# Foundation Radius

Primitive corner radius tokens for buttons, inputs, cards, and containers. Values are sourced
from design tokens and resolve at runtime.

<FoundationRadiusDoc />
